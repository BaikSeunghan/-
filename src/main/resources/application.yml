spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://34.64.105.170:5432/practice
    username: shbaik
    password: 1128!@

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    hibernate:
      ddl-auto: none
      use-new-id-generator-mappings: false
      order_inserts: true
      order_updates: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        type: trace

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace